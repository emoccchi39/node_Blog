<!DOCTYPE html>
<html lang="ja">
<head>
    <title>編集ページ</title>
    <%- include("./head"); %>
</head>
<body>
    <%- include("./header"); %>
    <main>
        <div>
            <h1>記事を書く</h1>
            <div>
                <form action="/blog/update/<%= singleBlog._id %>" method="post">
                    <label for="title">タイトル</label>
                    <input type="text" name="title" value="<%= singleBlog.title %>">
                    <label for="summary">要約</label>
                    <input type="text" name="summary" value="<%= singleBlog.summary %>">
                    <label for="image">イメージ</label>
                    <input type="text" name="image" placeholder="/img1.jpg" value="<%= singleBlog.image %>" required>
                    <label for="textBody">本文</label>
                    <textarea name="textBody" rows="15" required><%= singleBlog.textBody %></textarea>
                    <br>
                    <button type="submit">投稿</button>
                </form>
            </div>
        </div>
    </main>
    <%- include("./footer"); %>
</body>
</html>